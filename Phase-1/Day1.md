# C Programming Language
* In order to understand Computer We must understand some basic things thats communicate with Hardware basically your RAM Chips
* It is Procedural Language
* It is Language where the memory,stack,heap all them used by you
* Most Languages Foundation

# DataTypes
* In C There are different types of data types i see
  1. Integer(int)
     * Whole numbers
     * Range of numbers 2 power -31 to 2 power 31
     * format specifier %d
     * 4 bytes
  2. Float (float)
      * decimal numbers up to 6-7 precisions
      * format specifier %f
      * %.2f with 2 precision
      * 4 bytes
  3. Double (double)
      * decimal number (12-15 precisions)
      * format specifier %lf
      * format specifier %.3lf (3 precision)
      * 8 bytes
  4. Character (char)
      * single character
      * format specifier %c
      * shown by single quotes
      * 1 byte
  5. unsigned int
     * format specifier %u


  
